int x = 0;

export int main(int variable, int y) {

    int z;
    int x = variable + 3;
    y = variable - 3;

    for (int i = 0, 9, 2) {
        x = x + 1;
        main(variable * x, y);
    }

    return 0;
}
