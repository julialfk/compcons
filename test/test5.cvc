int x = 0;

export int main(int variable, int y) {
    int z;
    int x = variable + 3;
    float f = 2.5;
    y = variable - 3;
    f = f % 4;
    

    for (int i = 0, 9, 2) {
        x = x + 1;
        main(variable * x, y);
    }

    return x;
}
