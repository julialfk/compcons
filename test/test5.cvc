int x = 0;

export int main(float variable, int y) {

    int z;
    int x = v + 3;
    int z;
    y = variable - 3;

    for (int i = 0, 9, 2) {
        x = x + 1;
        main(variable * x, y);
    }

    return 0;
}
